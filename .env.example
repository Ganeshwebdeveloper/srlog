# Environment Variables for SR Logistics Fleet Management System

# Node Environment (development or production)
NODE_ENV=production

# Server Port (default: 5000)
PORT=5000

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
# For free hosting, use: Neon, Supabase, or Render's free PostgreSQL
DATABASE_URL=postgresql://user:password@host:port/sr_logistics

# Session Secret (generate a random string for production)
# Generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Optional: WebSocket Configuration
WS_PORT=5001

# Optional: If using external services
# Add your service API keys here
